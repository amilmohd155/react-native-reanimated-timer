import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    overflow: 'hidden',
  },
  section: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  separator: {
    textAlign: 'center',
  },
  otherDigits: {
    textAlign: 'center',
    fontVariant: ['tabular-nums'],
  },
});
