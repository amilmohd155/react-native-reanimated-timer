import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    overflow: 'hidden',
  },
  segment: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  otherDigits: {
    textAlign: 'center',
    fontVariant: ['tabular-nums'],
  },
  widthAuto: {
    width: 'auto',
  },
});
